# 大地图 View 层设计

## 1. 地图对象分类

大地图中的对象可以先按职责分为四组：

| 类别 | 典型对象 | 主要信息 |
| --- | --- | --- |
| 玩家对象 | 自己、盟友、盟主、敌方主城 | 等级、名称、联盟、关系色 |
| 威胁对象 | 普通怪、集结怪、迷雾源 | 等级、类型、集结状态 |
| 战略建筑 | 工程站、堡垒、要塞、王座、炮塔 | 活动阶段、保护、占领方、倒计时 |
| 资源对象 | 普通资源地、联盟资源地 | 资源类型、等级、归属、采集状态 |

关系色必须在全局统一，例如自己、盟友、盟主、敌方分别使用稳定的颜色语义，避免同一颜色在不同地图层级表达不同关系。

## 2. 缩放不是等比缩小

地图可拆成操作层、多个缩略层和总览层。层级越高，越应突出战略信息，减少实体细节。

### 操作层

- 展示完整模型、名称、等级、联盟、占领人和精确倒计时。
- 支持选中对象并执行攻击、侦察、采集、增援等操作。
- 需要完整表达保护罩、领地底座、待机动画和采集标识。

### 缩略层

- 主城、怪物和资源地逐步从模型切换为图标或标签。
- 敌方主城、普通怪物、普通资源点可按层级逐步隐藏。
- 战略建筑仍需保留可识别的轮廓和归属信息。
- “威胁”和“资源”筛选器决定哪些对象参与渲染。

### 总览层

- 关注王座、堡垒、要塞、联盟势力和资源富饶度。
- 普通资源点不再逐个显示，而是聚合成色阶或热区。
- 点击位置先给出搜索标记，再进入对应缩略层，保证空间定位连续。

## 3. 信息优先级

同一对象在不同层级应保持“身份不丢、细节递减”：

1. 身份：这是什么对象。
2. 归属：属于谁，敌我关系是什么。
3. 状态：保护、争夺、占领、采集或集结。
4. 数值：等级、时间、容量等。
5. 装饰：完整模型、动画、特效和领地细节。

缩放时优先隐藏低优先级信息，而不是让所有文字一起变小。

## 4. 筛选与聚合

- 威胁筛选：优先显示怪物、集结目标和迷雾源。
- 资源筛选：操作层显示资源点，总览层改为资源富饶度。
- 同屏对象过多时，可使用聚合、分桶、视口裁剪和同类合并。
- 筛选状态应在缩放后保持，避免玩家每次切层都要重新选择。

## 5. View 层的数据接口

View 不应自己推导活动规则，只消费标准化状态：

```text
MapObjectViewData
├─ object_id / object_type
├─ relation              自己、盟友、敌方、中立
├─ display_level         当前缩放层允许的表现等级
├─ icon_or_model
├─ name / alliance_tag / level
├─ phase                 未解锁、报名、保护、争夺、结束
├─ owner
├─ remaining_time
└─ interaction_flags     可攻击、可采集、可进入等
```

这样可以避免同一套占领判断在工程站、堡垒和王座的 View 中重复实现。
