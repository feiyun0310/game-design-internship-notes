# 大地图优化

## 问题归纳

原始优化记录集中在两个方面：缩放后的视觉保真，以及总览层的战略信息完整性。

### 缩略层

- 联盟要塞不应在缩放后退化为模糊或失真的占位表现，应使用专门的低细节模型或清晰图标。
- 炮台在缩略层不宜保留操作层的白色保护罩；保护状态可以改用更轻量的图标或颜色表达。
- 不同缩略层中的同类战略建筑应遵守同一套表现规则。

### 总览层

- 圣地、王座、堡垒等中立建筑被联盟占领后，应展示占领联盟的旗帜和简称。
- 总览层的空间很有限，可以隐藏占领者个人名称，但不能丢失联盟归属。
- 资源信息应从单点对象改为富饶度分区，颜色越深代表资源价值越高。

## 技术与表现策略

### 1. LOD 分级

为每类对象明确多个细节等级：

- LOD0：完整模型、动画、阴影、保护罩和精确标签。
- LOD1：低模、简化动画、核心标签。
- LOD2：图标、阵营色、等级或状态徽记。
- LOD3：聚合点、热区、领地色块。

切换阈值应使用滞回区间，避免镜头在边界附近移动时频繁闪烁。

### 2. 信息与模型解耦

模型可以降级，但归属、危险和活动状态不能跟着丢失。把“模型层”和“信息标记层”分开管理，可单独控制旗帜、名称、倒计时和状态徽记。

### 3. 性能优化

- 只处理视口内对象，并给边缘预留少量缓冲区。
- 静态对象使用批处理或实例化；频繁出现的标记使用对象池。
- 倒计时不必逐帧更新，按秒刷新即可。
- 远距离对象减少骨骼动画、阴影和透明特效。
- 地图数据按区域分块加载，镜头移动时预加载邻区。

## 验收用例

| 场景 | 操作 | 预期 |
| --- | --- | --- |
| 要塞缩放 | 从操作层缩小到三个缩略层 | 模型或图标清晰，无模糊占位 |
| 炮台保护 | 在保护阶段连续缩放 | 操作层可见保护表现，缩略层无多余白罩 |
| 联盟占领 | 联盟占领中立建筑后进入总览层 | 展示正确旗帜和联盟简称 |
| 归属切换 | 建筑被另一联盟夺取 | 标识及时刷新，无旧旗帜残留 |
| 高频缩放 | 在阈值附近反复缩放 | 不闪烁、不重复创建对象、帧率稳定 |
| 筛选保持 | 开启资源筛选后改变缩放层级 | 筛选状态保持，表现按层级变化 |
