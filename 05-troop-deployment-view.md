# 选兵出战界面

选兵出战页不是一个单纯的兵力滑杆，而是把目标、队长、英雄、士兵、预设、消耗和出征校验串成一次完整决策。

## 1. 页面结构

### 顶部：任务与预设

- 有目标时显示进攻或采集对象；无目标时显示“出战部队选择”。
- 已保存预设可一键切换阵容，并给出成功反馈。
- 空预设提示先配置；未解锁预设展示解锁条件。
- 保存时选择槽位，并明确提示会覆盖原方案。

### 中部：战斗单位

- 队长：上阵开关、角色形象、队长增益说明。
- 英雄：兵种专属槽位和通用槽位，显示品质、等级、星级和出征状态。
- 士兵：按兵种和等级排序，支持加减、拖动数量条、快速选择与均衡配置。
- 战力：总战力、带兵数量和详细加成入口。

### 底部：结果与行动

- 展示行军时间和体力消耗。
- 特殊减耗要同时展示原值与减免后数值。
- 行军按钮负责最终校验，成功后关闭编队页并进入行军状态。

## 2. 英雄槽位规则

示例结构是三个兵种专属槽位加两个通用槽位：

1. 英雄优先进入对应兵种的空专属槽位。
2. 专属槽位已占用时，尝试进入通用槽位。
3. 所有合法槽位都满时，拒绝上阵并说明原因。
4. 已在其他队列出征的英雄不可重复使用。
5. 已上阵英雄需要有明显选中标记，并支持再次点击移除。

全军英雄的落位优先级需要额外配置，不能依赖显示顺序隐式决定。

## 3. 士兵数量交互

数量应满足以下不变量：

```text
0 ≤ 单类已选数量 ≤ 单类可用数量
所有兵种已选总量 ≤ 队列容量
```

- 减少到 0 后减号置灰。
- 增加到可用上限或队列容量后加号置灰。
- 拖动、加减按钮和快速选择必须共用同一计算逻辑。
- 没有可上阵士兵时展示明确空状态和训练入口。

## 4. 增益与克制

- 队长增益只有队长上阵时生效。
- 英雄加成应按兵种、远征、采集、打野、攻城等场景分类。
- 克制关系必须统一，例如“坦克克机动、机动克飞行、飞行克坦克”。
- 正负号属于数值语义的一部分。诸如“降低敌方防御”必须显示减号，不能只依赖文案说明。

## 5. 出征前校验

建议固定校验顺序：

1. 目标仍然有效且可到达。
2. 队列槽位可用。
3. 英雄没有被其他队列占用。
4. 至少存在一个合法战斗单位。
5. 士兵数量不超库存和容量。
6. 体力或其他消耗足够。
7. 当前状态允许攻击、采集、集结或增援。

校验失败时保留玩家已编辑的阵容，只提示需要修正的项目。

## 6. 预设保存的数据

预设不应保存临时战力结果，而应保存选择意图：队长是否上阵、英雄 ID 与槽位、兵种选择策略、士兵数量或比例策略。载入时根据当前库存、伤兵和英雄占用状态重新计算，并提示被自动调整的部分。
