# 策划拆解方法

## 从体验到方案的四步法

### 1. 记录可验证的事实

先记录玩家做了什么、系统展示了什么、结果是什么。避免一开始就写“体验不好”，而应写成可验证描述，例如：

- 玩家点击升级后，材料不足弹出来源列表。
- 地图缩小到总览层后，中立建筑仍可见，但占领方信息消失。
- 编队槽位已满时，英雄列表只显示当前已上阵英雄。

### 2. 识别设计目标

同一个表现可能服务于不同目标：降低认知负担、促进养成、引导付费、提高联盟协作，或缩短重复操作。设计稿应明确主目标，否则后续无法判断取舍是否合理。

### 3. 建立状态与规则

把需求改写成“对象 + 状态 + 条件 + 结果”：

> 当建筑处于保护阶段时，展示保护罩和倒计时；保护结束后切换到争夺状态，并替换状态文案。

这一写法可以直接支持程序实现、测试用例和验收。

### 4. 补齐异常与反馈

每个操作至少检查以下结果：

- 成功：状态、数值和动画如何变化。
- 资源不足：缺少什么，是否提供获取入口。
- 条件不足：显示锁定原因还是直接隐藏。
- 重复操作：是否需要防连点、加载态或二次确认。
- 边界状态：满级、满星、槽位已满、无可用单位时如何展示。

## 一份可落地的界面需求应包含什么

| 模块 | 必须说明的内容 |
| --- | --- |
| 页面结构 | 顶部信息区、中部内容区、底部操作区、弹窗与浮层 |
| 元素定义 | 文案、图标、数值、底框、动效及数据来源 |
| 展示条件 | 可见、隐藏、置灰、高亮、红点、锁定 |
| 点击逻辑 | 跳转、弹窗、状态变化、网络请求、二次确认 |
| 数据刷新 | 初次进入、切换对象、操作成功、服务器推送时如何刷新 |
| 异常反馈 | 材料不足、条件不足、请求失败、空数据、超上限 |
| 验收标准 | 前置条件、操作步骤、预期表现和边界用例 |

## 迭代设计稿时应关注的变化

多版英雄系统稿体现了一个常见演进过程：

1. 第一版先覆盖功能范围和主要入口。
2. 修改版补充展示状态、锁定条件、升级来源、红点和动画反馈。
3. 后续版本进一步压缩重复描述，把页面共性抽成统一规则，并将星级、技能、装备等子系统拆开。

真正的“完善”不是继续堆按钮，而是减少歧义、统一状态定义，并让每个规则只在一个位置维护。
